<div class="d-flex justify-content-center align-items-center height-100vh width-100pc">
  <h1 class="font-size-7em m-0 font-family-DINpro"><%= (Time.now.hour < 10) ? "0#{Time.now.hour}" : Time.now.hour %> <span class="color-gold">:</span> <%= (Time.now.min < 10) ? "0#{Time.now.min}" : Time.now.min %></h1>
</div>

